repositories {
    jcenter()
    flatDir {
        dirs 'libs/'
    }
}

dependencies {
    compile(name: 'fyber-chartboost-6.5.0-r1', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
