repositories {
    jcenter()
    flatDir {
        dirs 'libs/'
    }
}

dependencies {
    compile(name: 'fyber-unityads-2.0.5-r1', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
